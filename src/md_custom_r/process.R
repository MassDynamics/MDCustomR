library(MDCustomR)

run_transform_intensities <- function(){

  output <- data.frame(
    Test = c("First"),
    Message = c("a message")
  )

  return_object <- list(output = as.data.frame(output))
  return(return_object)
}
